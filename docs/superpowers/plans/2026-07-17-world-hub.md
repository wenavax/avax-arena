# World Hub Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Kasabaya 9 oyun binası; E → oyun same-origin iframe overlay'de dünyadan çıkmadan açılır (spec: `docs/superpowers/specs/2026-07-17-world-hub-design.md`).

**Architecture:** Tek doğruluk kaynağı `hubGames.ts` kayıt defteri; Phaser tarafı `hub_<id>` interact tile'ları + `window` custom event köprüsü; React tarafı `GameOverlay` (iframe + `?embed=1`); kök layout'ta `EmbedMode` body sınıfıyla site kromunu gizler. Overlay açıkken sahne `scene.pause()` + müzik stop.

**Tech Stack:** Next.js 14 (basePath `/avalanche`), Phaser 3, React 18, Playwright (headless doğrulama, `~/.npm/_npx/e41f203b7505f1fb/node_modules/playwright` üzerinden import), `npx tsx` (birim scriptleri).

**Kritik bilgiler (kod okumadan bilinemez):**
- basePath tuzağı: iframe `src` ve elle kurulan URL'ler `/avalanche` önekli olmalı (`src={'/avalanche' + game.url + '?embed=1'}`).
- Kasaba haritası: `buildTownTiles()` 32 sütun × 26 satır; spawn `initZone(tiles, 16, 22)`; mevcut yapılar: elder kulesi ~satır 3-9/sütun 3-7, shop satır 3-8/sütun 23-28, inn satır ~21/sütun 5, orman çıkışı satır 11/sütun 31 ve satır ~0/sütun ~10. `tile(biome, height, collision, extra)` fabrikası dosyanın içinde.
- Kapı kalıbı: alt duvar satırında `tile('wood', 2, false, { interact: '...' })` (shop kapısı `tiles[8][25]` örneği).
- `IsoBaseScene.onInteract` `exit_` öneklileri elemeden geçirir; E etkileşimi `update()` içinde `frozen` guard'lı — overlay açıkken frozen olduğundan çifte tetik zaten engelli.
- Müzik: `IsoBaseScene.create` ~satır 214: `const zoneMusic = musicMap[this.scene.key] || 'town'; music.play(zoneMusic);`.
- Dünya girişi: `components/game/WorldLoginGate.tsx:275-277` → `if (enterGame) { return <PhaserGame />; }`.
- Test altyapısı: jest YOK. Birim = `npx tsx` scriptleri (`scripts/cardgame-engine-test.ts` kalıbı: sayaçlı ok(), process.exit). Tarayıcı = Playwright scriptleri scratchpad'de değil `frontend/scripts/` altında kalıcı tutulur.

---

## Dosya haritası

| Dosya | Sorumluluk |
|---|---|
| Create `frontend/lib/game/hub/hubGames.ts` | 9 oyunun kayıt defteri (id, ad, ikon, url, bina boyutu/çatısı, kapı koordinatı) |
| Create `frontend/components/game/GameOverlay.tsx` | iframe overlay (aç/kapat event'leri, 10sn timeout fallback, safe-area) |
| Create `frontend/components/EmbedMode.tsx` | `?embed=1` → `body.embed` |
| Modify `frontend/app/layout.tsx` | `<EmbedMode />` ekle |
| Modify `frontend/app/globals.css` | `.embed [data-chrome]` gizleme kuralları |
| Modify `frontend/components/layout/Sidebar.tsx` | Sidebar + MobileTopBar köklerine `data-chrome` |
| Modify `frontend/components/game/WorldLoginGate.tsx` | `<GameOverlay />`'i PhaserGame'in yanına render et |
| Modify `frontend/lib/game/PhaserGame.tsx` | `(window as any).__frostbiteGame = game` (E2E erişimi) |
| Modify `frontend/lib/game/iso/IsoBaseScene.ts` | `zoneMusicKey` alanı + `openHubGame()` + `hub_` yakalama |
| Modify `frontend/lib/game/scenes/IsoTownScene.ts` | `buildTownTiles` export + 9 bina + tabela metinleri |
| Create `frontend/scripts/hub-registry-test.ts` | kayıt defteri bütünlük birim testi |
| Create `frontend/scripts/hub-town-check.ts` | kapı/erişilebilirlik/mevcut-interact-korunumu testi |
| Create `frontend/scripts/hub-embed-smoke.mjs` | 9 sayfa `?embed=1` smoke |
| Create `frontend/scripts/hub-e2e.mjs` | overlay aç/kapat + resume E2E (geçici harness rotası üzerinden) |
| Create (geçici) `frontend/app/world/hub-test/page.tsx` | login-gate'siz harness — SON TASK'TA SİLİNİR |

---

### Task 1: Oyun kayıt defteri

**Files:**
- Create: `frontend/lib/game/hub/hubGames.ts`
- Test: `frontend/scripts/hub-registry-test.ts`

- [ ] **Step 1: Failing test'i yaz**

```ts
// frontend/scripts/hub-registry-test.ts
import { HUB_GAMES } from '/Users/hts_bot/avax-arena/frontend/lib/game/hub/hubGames';

let fails = 0;
const ok = (c: boolean, m: string) => { console.log(c ? `  ✓ ${m}` : `  ✗ ${m}`); if (!c) fails++; };

ok(HUB_GAMES.length === 9, `9 oyun kayıtlı (${HUB_GAMES.length})`);
ok(new Set(HUB_GAMES.map(g => g.id)).size === 9, 'id benzersiz');
ok(new Set(HUB_GAMES.map(g => `${g.door.tx},${g.door.ty}`)).size === 9, 'kapı koordinatı benzersiz');
ok(HUB_GAMES.every(g => g.url.startsWith('/') && !g.url.startsWith('/avalanche')), 'url / ile başlar, basePath İÇERMEZ');
ok(HUB_GAMES.every(g => g.door.tx >= 1 && g.door.tx <= 30 && g.door.ty >= 1 && g.door.ty <= 24), 'kapılar 32x26 harita içinde');
ok(HUB_GAMES.every(g => g.size.w >= 3 && g.size.h >= 3), 'binalar en az 3x3');

console.log(fails ? `${fails} FAIL` : 'ALL PASS');
process.exit(fails ? 1 : 0);
```

- [ ] **Step 2: Testi çalıştır — FAIL bekle**

Run: `cd /Users/hts_bot/avax-arena/frontend && npx tsx scripts/hub-registry-test.ts`
Expected: `Cannot find module ... hubGames`

- [ ] **Step 3: Kayıt defterini yaz**

```ts
// frontend/lib/game/hub/hubGames.ts
// World Hub — kasaba binalarının tek doğruluk kaynağı.
// door = kapı tile'ı (interact: 'hub_<id>'); bina dikdörtgeni kapıdan türetilir:
// kapı alt duvar satırının ortasıdır (rect: rows [door.ty-size.h+1 .. door.ty],
// cols [door.tx-floor(w/2) .. door.tx+ceil(w/2)-1]).

export interface HubGame {
  id: string;
  name: string;     // tabela metni
  icon: string;     // tabela emojisi
  url: string;      // basePath'siz route ('/cardgame' gibi)
  accent: string;   // overlay başlık rengi (css)
  roof: string;     // bina çatı biome'u (mevcut tile biome'larından)
  size: { w: number; h: number };
  door: { tx: number; ty: number };
}

export const HUB_GAMES: HubGame[] = [
  { id: 'arena',        name: 'ARENA',         icon: '⚔️', url: '/battle',              accent: '#ed2f39', roof: 'roof_red',  size: { w: 5, h: 4 }, door: { tx: 26, ty: 15 } },
  { id: 'cardgame',     name: 'CAR(D) GAME',   icon: '🏎️', url: '/cardgame',            accent: '#f5c542', roof: 'wood',      size: { w: 5, h: 4 }, door: { tx: 20, ty: 15 } },
  { id: 'battleroyale', name: 'BATTLE ROYALE', icon: '👑', url: '/world/battle-royale', accent: '#c084fc', roof: 'roof_red',  size: { w: 4, h: 4 }, door: { tx: 15, ty: 5 }  },
  { id: 'expeditions',  name: 'EXPEDITIONS',   icon: '🎲', url: '/expeditions',         accent: '#a78bfa', roof: 'wood',      size: { w: 4, h: 3 }, door: { tx: 26, ty: 19 } },
  { id: 'adventures',   name: 'ADVENTURES',    icon: '🧭', url: '/world/adventures',    accent: '#6ee7a0', roof: 'wood',      size: { w: 4, h: 3 }, door: { tx: 12, ty: 19 } },
  { id: 'swap',         name: 'SWAP',          icon: '💱', url: '/swap',                accent: '#2dd4bf', roof: 'roof_red',  size: { w: 4, h: 3 }, door: { tx: 3,  ty: 14 } },
  { id: 'marketplace',  name: 'NFT MARKET',    icon: '🏪', url: '/marketplace',         accent: '#fb7185', roof: 'wood',      size: { w: 4, h: 3 }, door: { tx: 3,  ty: 18 } },
  { id: 'launchpad',    name: 'LAUNCHPAD',     icon: '🚀', url: '/launchpad',           accent: '#f97316', roof: 'roof_red',  size: { w: 4, h: 4 }, door: { tx: 9,  ty: 23 } },
  { id: 'nftscore',     name: 'NFT SCORE',     icon: '💎', url: '/nft-score',           accent: '#c084fc', roof: 'wood',      size: { w: 3, h: 3 }, door: { tx: 20, ty: 22 } },
];

export const HUB_INTERACT_PREFIX = 'hub_';
```

⚠️ Kapı koordinatları Task 5'in erişilebilirlik testine göre KAYDIRILABİLİR — kayıt defteri tek doğruluk kaynağı olduğundan yalnız bu dosya değişir.

- [ ] **Step 4: Testi çalıştır — PASS bekle**

Run: `npx tsx scripts/hub-registry-test.ts`
Expected: `ALL PASS`

- [ ] **Step 5: Commit**

```bash
cd /Users/hts_bot/avax-arena && git add frontend/lib/game/hub/hubGames.ts frontend/scripts/hub-registry-test.ts
git commit -m "feat(world-hub): oyun kayıt defteri + bütünlük testi"
```

---

### Task 2: Embed modu (site kromunu gizle)

**Files:**
- Create: `frontend/components/EmbedMode.tsx`
- Modify: `frontend/app/layout.tsx` (body içine `<EmbedMode />`)
- Modify: `frontend/components/layout/Sidebar.tsx` (Sidebar + MobileTopBar köklerine `data-chrome`)
- Modify: `frontend/app/globals.css`
- Test: `frontend/scripts/hub-embed-smoke.mjs`

- [ ] **Step 1: Bileşeni yaz**

```tsx
// frontend/components/EmbedMode.tsx
'use client';
import { useEffect } from 'react';

/* ?embed=1 ile yüklenen sayfa site kromu (sidebar, mobil üst bar) olmadan
 * tam viewport render edilir — World Hub overlay iframe'leri için.
 * Sınıf body'de kalır; iframe içi SPA gezinmeleri de embed kalır. */
export function EmbedMode() {
  useEffect(() => {
    if (new URLSearchParams(window.location.search).get('embed') === '1') {
      document.body.classList.add('embed');
    }
  }, []);
  return null;
}
```

- [ ] **Step 2: layout.tsx'e ekle**

`frontend/app/layout.tsx` — `<body className="font-sans antialiased min-h-screen">` açılışından hemen sonra:

```tsx
import { EmbedMode } from '@/components/EmbedMode';
// ... body içinde ilk çocuk olarak:
<EmbedMode />
```

- [ ] **Step 3: Sidebar köklerine data-chrome ekle**

`frontend/components/layout/Sidebar.tsx` içinde `Sidebar` bileşeninin döndürdüğü kök elemana ve `MobileTopBar` kök elemanına `data-chrome=""` attribute'u ekle (mevcut className'lere dokunma — kökü bul, tek attribute ekle).

- [ ] **Step 4: globals.css kuralları**

`frontend/app/globals.css` sonuna:

```css
/* ── World Hub embed modu: iframe içinde site kromu gizlenir ── */
body.embed [data-chrome] { display: none !important; }
body.embed main { padding: 0 !important; }
```

- [ ] **Step 5: Smoke testi yaz**

```js
// frontend/scripts/hub-embed-smoke.mjs
// 9 hub sayfası ?embed=1 ile: sidebar yok + 0 konsol hatası.
// Kullanım: node scripts/hub-embed-smoke.mjs [baseUrl]  (default http://localhost:3000)
import { chromium } from 'file:///Users/hts_bot/.npm/_npx/e41f203b7505f1fb/node_modules/playwright/index.mjs';

const BASE = process.argv[2] || 'http://localhost:3000';
const URLS = ['/battle', '/cardgame', '/world/battle-royale', '/expeditions', '/world/adventures', '/swap', '/marketplace', '/launchpad', '/nft-score'];

const browser = await chromium.launch();
const ctx = await browser.newContext({ viewport: { width: 1024, height: 720 } });
let fails = 0;
for (const u of URLS) {
  const page = await ctx.newPage();
  const errs = [];
  page.on('pageerror', e => errs.push(String(e)));
  page.on('console', m => { if (m.type() === 'error') errs.push(m.text()); });
  await page.goto(`${BASE}/avalanche${u}?embed=1`, { waitUntil: 'domcontentloaded', timeout: 45000 });
  await page.waitForTimeout(4000);
  const chromeVisible = await page.evaluate(() =>
    [...document.querySelectorAll('[data-chrome]')].some(el => el.getClientRects().length > 0));
  const real = errs.filter(e => !/net::ERR|Failed to fetch|walletconnect|favicon|status of 40/i.test(e));
  const pass = !chromeVisible && real.length === 0;
  console.log(`${pass ? '✓' : '✗'} ${u} — chrome:${chromeVisible ? 'GÖRÜNÜR' : 'gizli'} err:${real.length}`);
  real.slice(0, 3).forEach(e => console.log('   ', e.slice(0, 160)));
  if (!pass) fails++;
  await page.close();
}
await browser.close();
console.log(fails ? `${fails} FAIL` : 'ALL PASS');
process.exit(fails ? 1 : 0);
```

- [ ] **Step 6: Dev server ile çalıştır — PASS bekle**

```bash
cd /Users/hts_bot/avax-arena/frontend
(npm run dev > /dev/null 2>&1 &) && sleep 8
node scripts/hub-embed-smoke.mjs
```
Expected: 9 satır ✓ + `ALL PASS`. (Not: dev server'ı sonraki task'lar için açık bırak.)

- [ ] **Step 7: Commit**

```bash
cd /Users/hts_bot/avax-arena && git add frontend/components/EmbedMode.tsx frontend/app/layout.tsx frontend/components/layout/Sidebar.tsx frontend/app/globals.css frontend/scripts/hub-embed-smoke.mjs
git commit -m "feat(world-hub): embed modu — ?embed=1 site kromunu gizler + 9 sayfa smoke"
```

---

### Task 3: GameOverlay bileşeni + geçici harness rotası

**Files:**
- Create: `frontend/components/game/GameOverlay.tsx`
- Create: `frontend/app/world/hub-test/page.tsx` (GEÇİCİ — Task 7'de silinir)
- Modify: `frontend/lib/game/PhaserGame.tsx` (window handle)

- [ ] **Step 1: GameOverlay'i yaz**

```tsx
// frontend/components/game/GameOverlay.tsx
'use client';
import { useCallback, useEffect, useState } from 'react';
import { HUB_GAMES, HubGame } from '@/lib/game/hub/hubGames';

/* World Hub overlay: Phaser 'hub-open-game' event'iyle açılır, oyunu
 * same-origin iframe'de (?embed=1) çalıştırır. Kapanınca iframe DOM'dan
 * tamamen sökülür ve 'hub-overlay-closed' yayınlanır (sahne resume eder).
 * basePath tuzağı: iframe src elle /avalanche önekli. */
export function GameOverlay() {
  const [game, setGame] = useState<HubGame | null>(null);
  const [loaded, setLoaded] = useState(false);
  const [failed, setFailed] = useState(false);

  useEffect(() => {
    const onOpen = (e: Event) => {
      const id = (e as CustomEvent).detail?.gameId as string;
      const g = HUB_GAMES.find(x => x.id === id);
      if (g) { setGame(g); setLoaded(false); setFailed(false); }
    };
    window.addEventListener('hub-open-game', onOpen);
    return () => window.removeEventListener('hub-open-game', onOpen);
  }, []);

  // iframe 10 sn'de load vermezse fallback — başlık çubuğu bizde, kilitlenmez
  useEffect(() => {
    if (!game || loaded || failed) return;
    const t = setTimeout(() => setFailed(true), 10000);
    return () => clearTimeout(t);
  }, [game, loaded, failed]);

  const close = useCallback(() => {
    setGame(null);
    window.dispatchEvent(new CustomEvent('hub-overlay-closed'));
  }, []);

  if (!game) return null;
  const src = `/avalanche${game.url}${game.url.includes('?') ? '&' : '?'}embed=1`;

  return (
    <div data-testid="hub-overlay" style={{
      position: 'fixed', inset: 0, zIndex: 1000, background: 'rgba(4,8,16,.96)',
      display: 'flex', flexDirection: 'column',
      paddingTop: 'env(safe-area-inset-top)', paddingBottom: 'env(safe-area-inset-bottom)',
      paddingLeft: 'env(safe-area-inset-left)', paddingRight: 'env(safe-area-inset-right)',
    }}>
      <div style={{
        display: 'flex', alignItems: 'center', justifyContent: 'space-between',
        padding: '8px 14px', background: '#0d1420', borderBottom: `2px solid ${game.accent}`,
      }}>
        <span style={{ color: game.accent, fontWeight: 700, fontFamily: 'monospace', fontSize: 14 }}>
          {game.icon} {game.name}
        </span>
        <button data-testid="hub-close" onClick={close} aria-label="Close" style={{
          background: 'rgba(255,255,255,.08)', color: '#cde', border: 0, borderRadius: 8,
          width: 44, height: 44, fontSize: 18, cursor: 'pointer',
        }}>✕</button>
      </div>
      {failed ? (
        <div style={{ flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', gap: 12, color: '#8899aa', fontFamily: 'monospace' }}>
          <span>Game failed to load.</span>
          <div style={{ display: 'flex', gap: 10 }}>
            <button onClick={() => { setFailed(false); setLoaded(false); setGame({ ...game }); }}
              style={{ background: game.accent, color: '#000', border: 0, borderRadius: 8, padding: '10px 18px', fontWeight: 700, cursor: 'pointer' }}>RETRY</button>
            <a href={src} target="_blank" rel="noreferrer"
              style={{ color: '#4dd0e1', alignSelf: 'center' }}>Open as page →</a>
          </div>
        </div>
      ) : (
        <iframe src={src} onLoad={() => setLoaded(true)} allow="clipboard-write"
          style={{ flex: 1, width: '100%', border: 0, background: '#0a0e1a' }} />
      )}
    </div>
  );
}
```

- [ ] **Step 2: PhaserGame'e window handle ekle**

`frontend/lib/game/PhaserGame.tsx` — Phaser game instance yaratıldığı yerin hemen sonrasına:

```ts
(window as unknown as Record<string, unknown>).__frostbiteGame = game;
```

(destroy/cleanup yolunda `delete (window as any).__frostbiteGame;` da ekle.)

- [ ] **Step 3: Geçici harness rotası**

```tsx
// frontend/app/world/hub-test/page.tsx
'use client';
/* GEÇİCİ World Hub test harness'ı — login gate'siz PhaserGame + GameOverlay.
 * Task 7'de SİLİNİR. */
import nextDynamic from 'next/dynamic';
import { GameOverlay } from '@/components/game/GameOverlay';

const PhaserGame = nextDynamic(() => import('@/lib/game/PhaserGame').then(m => m.PhaserGame), { ssr: false });

export default function HubTestPage() {
  return (<><PhaserGame /><GameOverlay /></>);
}
```

- [ ] **Step 4: Elle hızlı doğrulama**

Dev server açıkken: `curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/avalanche/world/hub-test` → `200`.

- [ ] **Step 5: Commit**

```bash
cd /Users/hts_bot/avax-arena && git add frontend/components/game/GameOverlay.tsx frontend/app/world/hub-test/page.tsx frontend/lib/game/PhaserGame.tsx
git commit -m "feat(world-hub): GameOverlay iframe bileşeni + geçici hub-test harness"
```

---

### Task 4: Phaser tarafı — openHubGame + hub_ interact

**Files:**
- Modify: `frontend/lib/game/iso/IsoBaseScene.ts`

- [ ] **Step 1: zoneMusicKey alanını yakala**

`IsoBaseScene.create` içinde (~satır 214) mevcut:
```ts
const zoneMusic = musicMap[this.scene.key] || 'town';
music.play(zoneMusic);
```
şu hale gelir:
```ts
const zoneMusic = musicMap[this.scene.key] || 'town';
this.zoneMusicKey = zoneMusic;
music.play(zoneMusic);
```
ve sınıf alanlarına (örn. `protected frozen` yanına):
```ts
protected zoneMusicKey = 'town';
```

- [ ] **Step 2: openHubGame metodunu ekle**

`IsoBaseScene`'e (freeze/unfreeze bölümünün yanına):

```ts
// ── World Hub: oyun binası kapısı — React overlay'ini açar ──
// Sahne duraklar (render+update), müzik durur; overlay kapanınca
// 'hub-overlay-closed' ile kaldığı yerden devam eder. MP socket'e
// dokunulmaz (presence düşmez, paused sahne update işlemez zaten).
protected openHubGame(gameId: string): void {
  this.freeze();
  music.stop();
  window.dispatchEvent(new CustomEvent('hub-open-game', { detail: { gameId } }));
  const onClosed = () => {
    window.removeEventListener('hub-overlay-closed', onClosed);
    this.scene.resume();
    music.play(this.zoneMusicKey);
    this.unfreeze();
  };
  window.addEventListener('hub-overlay-closed', onClosed);
  this.events.once('shutdown', () => window.removeEventListener('hub-overlay-closed', onClosed));
  // pause'u bir frame ertele: dispatch + freeze görselleri otursun
  this.time.delayedCall(50, () => { if (this.frozen) this.scene.pause(); });
}
```

(`music` importu dosyada zaten var — `import { music } from '../musicSystem'`; yoksa ekle.)

- [ ] **Step 3: hub_ önekini onInteract akışına bağla**

`IsoBaseScene.update` içindeki E-etkileşim bloğu `this.onInteract(tile, tx, ty)` çağırır; alt sınıflar override eder. Genel yakalama için `IsoBaseScene.onInteract`'ın hemen ÜSTÜNE ortak giriş noktası eklemek yerine, E-bloğunun `onInteract` çağrılarını saran küçük bir değişiklik yap: `update()` içindeki iki `this.onInteract(...)` çağrısını şuna çevir:

```ts
this.dispatchInteract(standTile, this.playerTx, this.playerTy);   // 1. çağrı
this.dispatchInteract(tile, ntx, nty);                            // 2. çağrı (komşu döngüsü)
```

ve yeni metod:

```ts
// hub_ kapıları tüm zone'larda ortak işlenir; kalan her şey alt sınıfın
// onInteract switch'ine gider
private dispatchInteract(tile: ZoneTile, tx: number, ty: number): void {
  if (tile.interact?.startsWith('hub_')) {
    this.openHubGame(tile.interact.slice(4));
    return;
  }
  this.onInteract(tile, tx, ty);
}
```

- [ ] **Step 4: tsc kontrolü**

Run: `cd /Users/hts_bot/avax-arena/frontend && npx tsc --noEmit 2>&1 | grep -vE "track3d|@types/three|fuji-ui-smoke|matchescrow-rehearsal"`
Expected: boş (bilinen 2 eski script hatası dışında yeni hata yok).

- [ ] **Step 5: Commit**

```bash
cd /Users/hts_bot/avax-arena && git add frontend/lib/game/iso/IsoBaseScene.ts
git commit -m "feat(world-hub): openHubGame — hub_ kapıları overlay açar, sahne pause/resume"
```

---

### Task 5: Kasabaya 9 bina

**Files:**
- Modify: `frontend/lib/game/scenes/IsoTownScene.ts`
- Test: `frontend/scripts/hub-town-check.ts`

- [ ] **Step 1: buildTownTiles'ı export et + binaları diktir**

`function buildTownTiles()` → `export function buildTownTiles()`.

Fonksiyonun SONUNA, `return tiles;` öncesine:

```ts
// =========================================================================
// WORLD HUB BİNALARI — kayıt defterinden türetilir (hubGames.ts tek kaynak)
// Kapı alt duvarın ortası; çakışan tile'lar (mevcut interact/deco) atlanır,
// hub-town-check.ts bunu ayrıca doğrular.
// =========================================================================
for (const g of HUB_GAMES) {
  const r1 = g.door.ty;                       // alt duvar satırı
  const r0 = r1 - g.size.h + 1;
  const c0 = g.door.tx - Math.floor(g.size.w / 2);
  const c1 = c0 + g.size.w - 1;
  for (let r = r0; r <= r1; r++) {
    for (let c = c0; c <= c1; c++) {
      if (r < 0 || r >= ROWS || c < 0 || c >= COLS) continue;
      if (tiles[r][c].interact || tiles[r][c].data) continue; // mevcut dokuya saygı
      const isWall = r === r0 || r === r1 || c === c0 || c === c1;
      tiles[r][c] = tile(isWall ? 'wall' : g.roof, isWall ? 4 : 5, true);
    }
  }
  // Kapı: alt duvarda, yürünebilir, hub interact
  tiles[r1][g.door.tx] = tile('wood', 2, false, { interact: `${HUB_INTERACT_PREFIX}${g.id}` });
  // Kapı önü yürünebilir olsun
  if (r1 + 1 < ROWS && tiles[r1 + 1][g.door.tx].collision && !tiles[r1 + 1][g.door.tx].interact) {
    tiles[r1 + 1][g.door.tx] = tile('dirt', 2, false);
  }
}
```

Dosya başına import:
```ts
import { HUB_GAMES, HUB_INTERACT_PREFIX } from '../hub/hubGames';
```

- [ ] **Step 2: Tabela metinleri (create içinde)**

`IsoTownScene.create()` içinde `initZone(...)` çağrısından sonra:

```ts
// Hub bina tabelaları — kapının üstünde oyun adı
for (const g of HUB_GAMES) {
  const pos = toScreen(g.door.tx, g.door.ty, 6);
  this.add.text(pos.x, pos.y - 8, `${g.icon} ${g.name}`, {
    fontSize: '9px', fontFamily: 'monospace', color: '#7fe3f5',
    stroke: '#000000', strokeThickness: 3,
  }).setOrigin(0.5).setDepth((g.door.tx + g.door.ty) * 10 + g.door.ty + 9);
}
```

(`toScreen` importu dosyada zaten var; yoksa `../iso/core`'dan ekle.)

- [ ] **Step 3: Harita bütünlük testini yaz**

```ts
// frontend/scripts/hub-town-check.ts
// Kasaba: 9 hub kapısı doğru interact'la var, spawn'dan BFS ile erişilebilir,
// ve mevcut kritik interact'lar (shop/npc/inn/elder/exit) korunmuş.
import { buildTownTiles } from '/Users/hts_bot/avax-arena/frontend/lib/game/scenes/IsoTownScene';
import { HUB_GAMES } from '/Users/hts_bot/avax-arena/frontend/lib/game/hub/hubGames';

let fails = 0;
const ok = (c: boolean, m: string) => { console.log(c ? `  ✓ ${m}` : `  ✗ ${m}`); if (!c) fails++; };

const tiles = buildTownTiles();
const ROWS = tiles.length, COLS = tiles[0].length;

// BFS: spawn (16,22)'den yürünebilir tile'lar
const seen = new Set<string>(['16,22']);
const q: [number, number][] = [[16, 22]];
while (q.length) {
  const [tx, ty] = q.shift()!;
  for (const [dx, dy] of [[0, 1], [0, -1], [1, 0], [-1, 0]]) {
    const nx = tx + dx, ny = ty + dy;
    if (nx < 0 || nx >= COLS || ny < 0 || ny >= ROWS) continue;
    const k = `${nx},${ny}`;
    if (seen.has(k) || tiles[ny][nx].collision) continue;
    seen.add(k); q.push([nx, ny]);
  }
}

for (const g of HUB_GAMES) {
  const t = tiles[g.door.ty][g.door.tx];
  ok(t.interact === `hub_${g.id}`, `${g.id} kapısı (${g.door.tx},${g.door.ty}) interact=${t.interact}`);
  // Kapının kendisi veya 4-komşusundan biri BFS'te (E etkileşimi komşudan da çalışır)
  const reachable = [[0, 0], [0, 1], [0, -1], [1, 0], [-1, 0]].some(([dx, dy]) =>
    seen.has(`${g.door.tx + dx},${g.door.ty + dy}`));
  ok(reachable, `${g.id} kapısına spawn'dan ulaşılabiliyor`);
}

// Mevcut doku korunumu
const allInteracts = tiles.flat().map(t => t.interact).filter(Boolean);
for (const must of ['shop', 'inn', 'elder_house']) {
  ok(allInteracts.includes(must), `mevcut '${must}' interact'i duruyor`);
}
ok(allInteracts.filter(i => i === 'npc').length >= 2, 'en az 2 NPC duruyor');
ok(allInteracts.some(i => i!.startsWith('exit_')), 'zone çıkışı duruyor');

console.log(fails ? `${fails} FAIL` : 'ALL PASS');
process.exit(fails ? 1 : 0);
```

- [ ] **Step 4: Testi çalıştır, kapı koordinatlarını gerekirse kaydır**

Run: `npx tsx scripts/hub-town-check.ts`
Expected: `ALL PASS`. Bir kapı erişilemez/çakışıyorsa YALNIZ `hubGames.ts`'teki `door` koordinatını komşu boş alana kaydır ve tekrar koştur (bina dikdörtgeni kapıdan türediği için başka değişiklik gerekmez). Not: `IsoTownScene` import'u Phaser'ı çekerse test `npx tsx` altında patlayabilir — bu durumda `buildTownTiles`'ı `lib/game/maps/townTiles.ts` diye Phaser'sız ayrı dosyaya taşı (tercih edilen çözüm) ve IsoTownScene oradan import etsin.

- [ ] **Step 5: Görsel kontrol (screenshot)**

Dev server açıkken Playwright tek seferlik: `http://localhost:3000/avalanche/world/hub-test` → 6 sn bekle → screenshot al, binaların kasabada göründüğünü ve tabelaları gözle doğrula.

- [ ] **Step 6: Commit**

```bash
cd /Users/hts_bot/avax-arena && git add frontend/lib/game/scenes/IsoTownScene.ts frontend/lib/game/maps/townTiles.ts frontend/scripts/hub-town-check.ts 2>/dev/null; git add frontend/lib/game/scenes/IsoTownScene.ts frontend/scripts/hub-town-check.ts
git commit -m "feat(world-hub): kasabaya 9 oyun binası + tabelalar + harita bütünlük testi"
```

---

### Task 6: WorldLoginGate entegrasyonu + uçtan uca E2E

**Files:**
- Modify: `frontend/components/game/WorldLoginGate.tsx:275-277`
- Test: `frontend/scripts/hub-e2e.mjs`

- [ ] **Step 1: GameOverlay'i gerçek dünyaya bağla**

`WorldLoginGate.tsx`:
```tsx
import { GameOverlay } from '@/components/game/GameOverlay';
// ...
if (enterGame) {
  return (<><PhaserGame /><GameOverlay /></>);
}
```

- [ ] **Step 2: E2E testini yaz**

```js
// frontend/scripts/hub-e2e.mjs
// Harness rotasında: her hub kapısı için oyuncuyu kapının önüne ışınla,
// interact'i tetikle → overlay doğru iframe src ile açılır → ✕ → sahne resume.
import { chromium } from 'file:///Users/hts_bot/.npm/_npx/e41f203b7505f1fb/node_modules/playwright/index.mjs';

const BASE = process.argv[2] || 'http://localhost:3000';
const browser = await chromium.launch();
const page = await browser.newPage({ viewport: { width: 1280, height: 720 } });
const errs = [];
page.on('pageerror', e => errs.push(String(e)));
page.on('console', m => { if (m.type() === 'error') errs.push(m.text()); });

await page.goto(`${BASE}/avalanche/world/hub-test`, { waitUntil: 'domcontentloaded' });
await page.waitForSelector('canvas', { timeout: 45000 });
await page.waitForTimeout(6000); // boot + CharacterSelect → not: harness sahnesi CharacterSelect'te açılırsa aşağıdaki evaluate town'a atlar

let fails = 0;
const ok = (c, m) => { console.log(c ? `  ✓ ${m}` : `  ✗ ${m}`); if (!c) fails++; };

// CharacterSelect'ten Town'a geç (save yoksa)
await page.evaluate(() => {
  const g = window.__frostbiteGame;
  const cs = g.scene.getScene('CharacterSelect');
  if (cs && g.scene.isActive('CharacterSelect')) { cs.scene.start('Town'); }
});
await page.waitForTimeout(4000);

// Kapı listesi sahnenin kendisinden toplanır (registry'yi ayrıca yüklemeye gerek yok):
const doors = await page.evaluate(() => {
  const g = window.__frostbiteGame;
  const town = g.scene.getScene('Town');
  const out = [];
  for (let ty = 0; ty < town.tiles.length; ty++)
    for (let tx = 0; tx < town.tiles[0].length; tx++) {
      const it = town.tiles[ty][tx].interact;
      if (it && it.startsWith('hub_')) out.push({ tx, ty, id: it.slice(4) });
    }
  return out;
});
ok(doors.length === 9, `sahnede 9 hub kapısı bulundu (${doors.length})`);

for (const d of doors) {
  const opened = await page.evaluate(({ tx, ty }) => {
    const g = window.__frostbiteGame;
    const town = g.scene.getScene('Town');
    town.playerTx = tx; town.playerTy = Math.min(town.tiles.length - 1, ty + 1);
    town.dispatchInteract
      ? town.dispatchInteract(town.tiles[ty][tx], tx, ty)
      : town.onInteract(town.tiles[ty][tx], tx, ty);
    return true;
  }, d);
  await page.waitForSelector('[data-testid="hub-overlay"] iframe', { timeout: 8000 });
  const src = await page.getAttribute('[data-testid="hub-overlay"] iframe', 'src');
  ok(opened && src.startsWith('/avalanche') && src.includes('embed=1'), `${d.id}: overlay açıldı, src=${src}`);
  const paused = await page.evaluate(() => window.__frostbiteGame.scene.getScene('Town').scene.isPaused());
  ok(paused, `${d.id}: sahne pause`);
  await page.click('[data-testid="hub-close"]');
  await page.waitForTimeout(400);
  const resumed = await page.evaluate(() => {
    const t = window.__frostbiteGame.scene.getScene('Town');
    return !t.scene.isPaused() && !t.frozen;
  });
  ok(resumed, `${d.id}: kapatınca resume + unfreeze`);
}

const real = errs.filter(e => !/net::ERR|Failed to fetch|walletconnect|favicon|status of 40/i.test(e));
ok(real.length === 0, `konsol hatasız (${real.length})`);
real.slice(0, 5).forEach(e => console.log('   ', e.slice(0, 160)));
await browser.close();
console.log(fails ? `${fails} FAIL` : 'ALL PASS');
process.exit(fails ? 1 : 0);
```

⚠️ Nüanslar (uygulayan kişi için):
- Sahne key'i `'Town'` — `musicMap` bu key'i kullanıyor; farklıysa `g.scene.scenes.map(s=>s.scene.key)` ile bul, testi düzelt.
- `dispatchInteract` private — JS'te çağrı çalışır; TS değil, sorun yok.
- CharacterSelect ekranı localStorage save durumuna göre değişebilir; evaluate bloğu save'siz durumda `scene.start('Town')` yapar. Gerekirse önce `localStorage.removeItem('frostbite_save')` + reload ile deterministik başlat.
- Overlay açılış sırası: `dispatchInteract` → `hub-open-game` → React state — `waitForSelector` 8sn toleransı yeterli.

- [ ] **Step 3: Çalıştır — PASS bekle**

Run: `node scripts/hub-e2e.mjs`
Expected: 9 kapı × 3 kontrol + genel kontroller, `ALL PASS`.

- [ ] **Step 4: Commit**

```bash
cd /Users/hts_bot/avax-arena && git add frontend/components/game/WorldLoginGate.tsx frontend/scripts/hub-e2e.mjs
git commit -m "feat(world-hub): GameOverlay world sayfasında + 9 kapı uçtan uca E2E"
```

---

### Task 7: Mobil smoke + harness silme + build + deploy

**Files:**
- Delete: `frontend/app/world/hub-test/page.tsx`
- (deploy: rsync + pm2, repo dışı)

- [ ] **Step 1: Mobil viewport smoke**

`hub-e2e.mjs`'i mobil viewport'la bir kez daha koş (script viewport'u argv'den almıyorsa geçici olarak `{ width: 393, height: 852, isMobile: true, hasTouch: true }` ile düzenleyip koş, sonra geri al — veya viewport'u `process.env.VP`'den okuyacak 3 satırlık parametrizasyonu kalıcı ekle, tercih edilen bu):

Run: `VP=393x852 node scripts/hub-e2e.mjs`
Expected: `ALL PASS`.

- [ ] **Step 2: Harness'ı sil**

```bash
rm -rf /Users/hts_bot/avax-arena/frontend/app/world/hub-test
```

- [ ] **Step 3: tsc + production build**

```bash
cd /Users/hts_bot/avax-arena/frontend
npx tsc --noEmit 2>&1 | grep -vE "track3d|@types/three|fuji-ui-smoke|matchescrow-rehearsal"   # boş beklenir
npm run build   # ✓ Compiled successfully beklenir
```

- [ ] **Step 4: Commit**

```bash
cd /Users/hts_bot/avax-arena && git add -A frontend/app/world && git commit -m "feat(world-hub): harness kaldırıldı — hub paketi tamam"
git push origin frozenfriends-mvp
```

- [ ] **Step 5: Deploy + prod smoke**

```bash
rsync -avz --delete --exclude='node_modules' --exclude='.env' --exclude='.env.local' \
  --exclude='data/frostbite.db' --exclude='data/frostbite.db-shm' --exclude='data/frostbite.db-wal' \
  --exclude='data/uploads' -e "ssh -i ~/.ssh/id_ed25519" \
  /Users/hts_bot/avax-arena/frontend/ root@5.189.173.167:/opt/frostbite/mainnet/frontend/
ssh -i ~/.ssh/id_ed25519 root@5.189.173.167 "pm2 restart frostbite-mainnet"
cd /Users/hts_bot/avax-arena/frontend && node scripts/hub-embed-smoke.mjs https://frostbite.pro
```
Expected: prod'da 9 embed sayfası `ALL PASS`.

- [ ] **Step 6: Kullanıcı adımı (raporla, bekleme)**

Canlı cüzdanlı doğrulama kullanıcıya bırakılır: world'e gir → Yarış Garajı'nda E → cardgame overlay'de cüzdan bağlı mı, MetaMask-in-iframe tx akıyor mu. Bu adımı rapora "senin gözünle" maddesi olarak yaz.

---

## Görev sırası ve bağımlılıklar

```
Task 1 (registry) ──► Task 3 (overlay) ──► Task 6 (entegrasyon+E2E) ──► Task 7 (kapanış)
        │                                    ▲
        ├─► Task 2 (embed modu) ─────────────┤
        ├─► Task 4 (Phaser openHubGame) ─────┤
        └─► Task 5 (kasaba binaları) ────────┘
```
Task 2/4/5 birbirinden bağımsız, Task 1'e bağımlı; paralel yürütülebilir (farklı dosyalar).

## Bilinçli sadeleştirme

Faz 1'de bina görseli = duvar+çatı tile'ları + kapı + tabela metni. Spec'in tema tablosundaki zengin dekorlar (meşaleler, damalı bayrak, mini roket, kristal küre) mevcut `deco` tile altyapısıyla (torch/barrel kalıbı) sonraki cila turunda eklenir — binaların yeri/kimliği bu planla sabitlenir, dekor eklemek yalnız `buildTownTiles`'a dokunur.
